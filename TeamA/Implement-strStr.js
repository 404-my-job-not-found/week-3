/**
 * @param {string} haystack
 * @param {string} needle
 * @return {number}
 */
const strStr = function (haystack, needle) {
  const needleIndex = haystack.indexOf(needle);
  return needleIndex;
};
